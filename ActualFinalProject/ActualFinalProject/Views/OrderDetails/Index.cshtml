@model IEnumerable<ActualFinalProject.OrderDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Item.ItemName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Order.ID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Item.ItemName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Order.ID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.OrderID, id2 = item.ItemID }) |
            @Html.ActionLink("Details", "Details", new { id = item.OrderID, id2 = item.ItemID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.OrderID, id2 = item.ItemID })
        </td>
    </tr>
}

</table>
